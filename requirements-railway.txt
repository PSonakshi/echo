# Requirements for Railway deployment (API server only, no Pathway)

# Environment
python-dotenv~=1.0

# Sentiment Analysis
vaderSentiment~=3.3.2

# Telegram Bot
python-telegram-bot~=21.0

# HTTP requests
requests~=2.31.0
aiohttp~=3.9.0

# Web Framework (for API server)
flask~=3.0.0
flask-cors~=4.0.0
flask-socketio~=5.3.0

# WebSocket support
python-socketio~=5.10.0
eventlet~=0.35.0

# Note: pathway is NOT included - it's only needed for the streaming pipeline
# which runs separately in Docker. The API server mocks pathway for compatibility.
